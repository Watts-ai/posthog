# Image tag to use for all PostHog services (default: latest)
IMAGE_TAG=latest

# PostHog Secret Key (generate one for production)
SECRET_KEY=unsafe-secret-key

# The URL where PostHog is accessible
SITE_URL=http://localhost:8000

# Optional: Sentry DSN for error tracking
# SENTRY_DSN=
